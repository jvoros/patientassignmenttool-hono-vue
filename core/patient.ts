// import ShortUniqueId from "short-unique-id";
// const uid = new ShortUniqueId({ length: 6 });

// const make = (params: { room: string; mode: Patient["mode"] }): Patient => {
//   return {
//     id: uid.rnd(),
//     room: params.room,
//     mode: params.mode,
//     time: Date.now(),
//   };
// };

// const changeRoom = (params: { patient: Patient; newRoom: string }): void => {
//   const { patient, newRoom } = params;
//   patient.room = newRoom;
// };

// export default {
//   make,
//   changeRoom,
// };
